import { StopOperatorPipe } from './stop-operator.pipe';

describe('StopOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new StopOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
